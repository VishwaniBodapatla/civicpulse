# CivicPulse - Database Connection
# Copy to .env and fill in your values

DB_SERVER=localhost
DB_NAME=CivicPulse
DB_USER=sa
DB_PASSWORD=your_password_here
DB_PORT=1433          # SQL Server default; use 5432 for PostgreSQL

DATA_DIR=data/
